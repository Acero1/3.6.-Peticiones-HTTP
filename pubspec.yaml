name: mi_app_nueva
description: Una nueva app Flutter para probar peticiones HTTP.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  firebase_core: ^3.1.0
  cloud_firestore: ^5.1.0

flutter:
  uses-material-design: true
